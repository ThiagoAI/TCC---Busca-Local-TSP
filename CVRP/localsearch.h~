#ifndef LOCALSEARCH_H_
#define LOCALSEARCH_H_

#include "point.h"

//Todos usam vizinhança 2-opt

//Steepest Descent
int local_search_SD (point** points,int size,int capacity);
//Best-First Search
int local_search_BFS (point** points,int size,int capacity);
//Hill Climbing
int local_search_HC (point** points,int size,int capacity);

#endif
